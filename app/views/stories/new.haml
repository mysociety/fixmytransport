.homepage
  .blog-link= link_to('What is this?', 'http://www.mysociety.org/2010/07/20/brief-encounters/', :target => '_blank')
  - cache('recent_stories') do 
    .latest-stories
      %a{:href => stories_url(:format => 'atom'), :class => 'rss-link'}
        %span= t(:feed)
        %img{:src => "/images/feed.png", :title => t(:feed), :alt => t(:stories_feed) }
      %h3= t(:latest_stories)
      = render :partial => 'story', :collection => Story.find_recent(5)
      %span.more-link=link_to t(:more_recent_stories), stories_url
  .new-story
    %h3= t(:got_a_story) 
    %p Is it comic, romantic, unfortunate or bizarre? 
    %p Where did it take place?
    - form_for @story, :url => find_stories_path do |form| 
      = render :partial => 'stories/form', :locals => { :form => form }
      != submit_tag t(:go)
