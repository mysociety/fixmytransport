<div id="banner">
    <a id="home" href="/">FixMyTransport</a>
	<div class="container">
		<h1><%= t('problems.choose_area.choose_area')%></h1>
	</div>
</div>
<div id="greybanner">
	<div class="container">
		<p><%= t('problems.choose_area.multiple_localities', :name => @name) %></p>
	</div>
</div>
<div id="main-content" class="container">
	<ul class='locality-list'>
	 <%- @areas.each do |area| %>
	    <li class='area'>
	      <%= link_to(area.full_name, find_bus_route_problems_url(:area => area.full_name, :route_number => params[:route_number], :area_type => area.class.to_s)) %>
	    </li>
	  <%- end %>
	</ul>
</div>