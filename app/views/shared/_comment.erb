<a href="#" class="thread-item">
  <span class="num"><%= index %></span>
  <span class="title">
  <%= raw comment_header(comment) %>
  <%- if comment.commented.is_a?(Problem) && comment.mark_fixed? %>
    <%= t('problems.show.marked_as_fixed')%>
  <%- end %>
  </span>
  <span class="close">close</span>
  <span class="date">
    <%= comment.created_at.localtime.to_s(:standard_with_date) %>
  </span>
</a>
<div class="thread-details">
	<div class="thread-copy">
		<%= raw simple_format(sanitize("#{comment.text}")) %>
	</div>
	<div class="thread-furniture">
		<div class="thumb"><img src="<%= comment.user.profile_photo.url(:large_thumb) %>" alt="" /></div>
	</div>
</div>