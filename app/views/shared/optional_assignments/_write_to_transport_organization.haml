- @campaign.problem.recipients.each do |recipient|
  .optional-assignment
    =link_to("Write to #{recipient.name} again", new_campaign_outgoing_message_path(@campaign, :recipient_type => recipient.type, :recipient_id => recipient.id))