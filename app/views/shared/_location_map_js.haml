- content_for :javascript do 
  != map_javascript_include_tags
:javascript
  var zoom = #{@zoom};
  var lat = #{@lat};
  var lon = #{@lon};
  var findOtherLocations = #{@find_other_locations};
  var linkType = '#{link_type}';
  var areaStops = #{location_stops_js(locations, main=true, small=false, link_type)};
  $(document).ready(function(){
     area_init();
  });