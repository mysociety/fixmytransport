<%= @assignment.data[:reason] %>

So, to send a mail to <%= raw @assignment.data[:name] %> right now, just follow this link:

<%= raw @link %>

[ This message was sent via FixMyTransport. Replies will be read by our kindly, mostly volunteer transport boffins. ]