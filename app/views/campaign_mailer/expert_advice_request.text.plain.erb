Hi transport experts!

<%= raw(@advice_request.user.name) %> would like some advice about the FixMyTransport campaign "<%= raw(@campaign.title) %>" (<%= raw @link %>).

<%= raw @advice_request.user.name %> says:

"<%= raw(@advice_request.text) %>"

To give some advice, follow this link: <%= raw @link %>

Yours,

The FixMyTransport team
