Hi<%= raw " #{@recipient.name}" %>,

<%= raw @advice_request.user.name %> would like some advice about how to "<%= raw(@campaign.title) %>".

<%= raw @advice_request.user.name %> is asking:

"<%= sanitize(raw(@advice_request.text)) %>"

To give some advice, follow this link: <%= raw @link %>

Thanks,
FixMyTransport
<%- if @unsubscribe_link %>

Unsubscribe: To stop receiving updates please follow this link: <%= raw @unsubscribe_link %>
<%- end %>