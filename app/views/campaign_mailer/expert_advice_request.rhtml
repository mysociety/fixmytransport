Hi transport experts!

<%= raw @advice_request.user.name %> would like some advice about the FixMyTransport campaign "<%= @campaign.title %>" (<%= @link %>).

<%= raw @advice_request.user.name %> says:

"<%= @advice_request.text %>"

To give some advice, follow this link: <%= @link %>

Yours,

The FixMyTransport team
