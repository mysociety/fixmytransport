Hello lovely transport boffins,

<%= raw(@advice_request.user.name) %> would like some advice about the FixMyTransport campaign "<%= raw(@campaign.title) %>" (<%= raw @advice_link %>).

<%= raw @advice_request.user.first_name %> says:

"<%= raw(@advice_request.text) %>"

About the campaign:

"<%= raw @campaign.description %>"

Please follow this link to give this user some precious pearls of transport wisdom : <%= raw @advice_link %>

Or, if you know straight away that this user should write immediately to someone in particular, please follow this link: <%= raw @assignment_link %>

Love and kisses,

The FixMyTransport Computer Brain
