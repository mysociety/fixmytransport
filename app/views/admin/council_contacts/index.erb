<%- @councils.each do |council| %>
  <div class='council'>
    <%= link_to(council.name, admin_url(admin_council_contacts_path(:area_id => council.id))) %>
    <%= t('admin.contact_count', :n => council.contacts.count, :count => council.contacts.count) %>
  </div>
<%- end %>
