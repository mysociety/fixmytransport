Hi <%= raw @user.name %>

A request to reset your password<%-if @action%><%= raw " and #{ raw @action}"%><%- end %> has been made. If you did not make this request, simply ignore this email.
If you did make this request just click the link below:

<%= raw @edit_password_reset_url %>

If your email program does not let you click on this link, copy
and paste it into your web browser and press return.

If you continue to have problems please contact us.

<%= render :partial => 'shared/mailer/model_confirmation', :locals => { :model => @unconfirmed_model }%>

Thanks,
FixMyTransport