<!DOCTYPE html>
<html lang='<%= I18n.locale %>' xmlns="http://www.w3.org/1999/xhtml" xmlns:fb="http://www.facebook.com/2008/fbml">
  <head>
    <title>
      <%= "#{t(:site_name)}#{@title ? ':' : ''} #{@title}" %>
    </title>
    <meta content='text/html; charset=utf-8' http-equiv='Content-type' />
    <%= yield :feeds %>
    <%= csrf_meta_tag %>
    <%= raw stylesheet_link_tag('core', 'fixmytransport', 'map', 'buttons', :cache => 'main') %>

	<!--[if lte IE 7]><%= raw stylesheet_link_tag('ie67') %><![endif]-->
	
	<!--[if lte IE 8]><%= raw stylesheet_link_tag('ie678') %><![endif]-->

    <%= yield :stylesheets %>
    <%= raw library_js_link %>
    <script type="text/javascript">
      var fmt_facebook_app_id = '<%= MySociety::Config.get('FACEBOOK_APP_ID','') %>';
    </script>
	<%= raw javascript_include_tag("http://use.typekit.com/fji8ime.js")%>
	<script type="text/javascript">try{Typekit.load();}catch(e){}</script>
  </head>
	<body>
		<%- if MySociety::Config.getbool('STAGING_SITE', true) %>
		<div class="staging-banner top-notice">
			<div class="container"><%= t(:development_site) %></div>
		</div>
		<%- end %>
    <%= yield :banner_content %>
		<div id="wrapper">
			<div id="top" class="container">
				<ul id="mysociety-tab">
					<li><a id="mysociety-logo" href="http://mysociety.org">MySociety</a></li>
					<li><a href="http://mysociety.org/donate">Donate</a></li>
					<li><a href="http://www.mysociety.org/projects/">Our Sites</a></li>
				</ul>
				<ul id="top-nav">
					<li><a href="<%= about_url %>">About</a></li>
					<li><a href="<%= feedback_url(:url => CGI.escape(request.request_uri)) %>">Feedback</a></li>
				</ul>
				<a id="logo" href="<%= root_url %>">Fix My Transport</a>
				<%- if current_user %>
				<p id="member-info">Hi <a href="<%= profile_path(current_user) %>"><%= current_user.name %></a>. <a href="<%= logout_path(:redirect => request.request_uri) %>">Logout</a></p>
				<%- else %>
				<p id="member-info">Hi Guest. <a class="auth" href="<%= login_path(:redirect => request.request_uri) %>">Sign in</a></p>
				<%- end %>
			</div>

			<%= render :partial => "shared/login" %>

			<%- [:notice, :error].each do |key| %>
					<%- unless flash[key].blank? %>
					<div class="container">
					<p class="<%= "flash #{key}" %>">
					  <%= raw flash[key] %>
					</p>
					</div>
					<%- end %>
			<%- end %>
			<%= yield %>
			<div id="goto-top-wrap" class="container">
				<a href="#top" class="goto-top right">Top of page</a>
			</div>
    	</div>

		<div id="footer">
			<div class="container">
				<p><%= raw t(:built_by, :mysociety_link => link_to('mySociety', 'http://mysociety.org'), :indigo_link => link_to('Indigo Trust', 'http://indigotrust.wordpress.com/'), :donor_link => link_to(t(:small_donors), 'http://mysociety.org/donate'), :esmee_link => link_to('Esmée Fairbairn Foundation', 'http://www.esmeefairbairn.org.uk')) %></p>
			</div>
		</div>
		<%= yield :javascript %>
		<%= raw main_js_link %>
		<!--[if lte IE 8]><%= raw javascript_include_tag('ie', :charset => 'utf-8') %><![endif]-->
		<%= render :partial => 'shared/tracking' %>
	</body>
</html>
